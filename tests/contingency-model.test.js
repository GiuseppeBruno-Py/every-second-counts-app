const test=require('node:test');const assert=require('node:assert/strict');const m=require('../contingency-model.js');
test('dados antigos continuam válidos',()=>{const x=m.normalizeAction({id:'a'});assert.equal(x.minimumVersion,null);assert.deepEqual(x.contingencies,[])});
test('versão mínima não conclui integralmente por padrão',()=>{assert.deepEqual(m.normalizeMinimum({description:' Ler resumo ',estimatedMinutes:'10',completionCriterion:'Uma nota'}),{description:'Ler resumo',estimatedMinutes:10,completionCriterion:'Uma nota',countsAsComplete:false})});
test('CRUD preserva id e evita duplicação no sync',()=>{let x=m.upsert([],{id:'c1',condition:'Pouco tempo',response:'Fazer mínimo'},'2026-01-01T00:00:00Z');x=m.upsert(x,{id:'c1',condition:'Menos de 20 min',response:'Fazer mínimo'},'2026-01-02T00:00:00Z');assert.equal(x.length,1);assert.equal(x[0].condition,'Menos de 20 min');assert.deepEqual(m.remove(x,'c1'),[])});
test('backfill remove ids duplicados e ordena',()=>{const x=m.normalizeAction({contingencies:[{id:'a',condition:'A',response:'B',order:2},{id:'b',condition:'C',response:'D',order:0},{id:'a',condition:'E',response:'F',order:1}]});assert.deepEqual(x.contingencies.map(v=>v.id),['b','a'])});
test('registra variantes',()=>{assert.deepEqual(m.variant('minimum'),{kind:'minimum',contingencyId:null});assert.deepEqual(m.variant('contingency','c1'),{kind:'contingency',contingencyId:'c1'})});
